{
  "name": "swiss-vault",
  "version": "1.0.0",
  "description": "Swiss Vault project",
  "main": "dist/server.js",
  "scripts": {
  "build": "npx vite build --root frontend && npx esbuild server/index-prod.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/server.js"
}
    "start": "node dist/server.js",
    "dev": "vite"
  },
  "dependencies": {
    "vite": "^5.1.0",
    "@vitejs/plugin-react": "^4.3.0",
    "esbuild": "^0.19.12",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "ISC"
}
