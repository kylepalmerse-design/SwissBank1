{
  "name": "swiss-vault",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx vite build && npx esbuild server/index-prod.ts --platform=node --bundle --format=esm --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "some-other-deps-if-нужны": "^x.x.x"
  },
  "devDependencies": {
    "vite": "^5.1.0",
    "@vitejs/plugin-react": "^4.3.0",
    "esbuild": "^0.19.12",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
